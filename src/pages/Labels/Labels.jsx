import { Header, Main, NavBar } from "../../components";

const Labels = () => (
  <div className="app-layout">
    <Header />
    <NavBar />
    <Main pageName="LABELS" />
  </div>
);

export { Labels };
